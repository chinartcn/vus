#// =============================================================================
#// 实机测试：Termux X11 显示 GuiLite 绘图基础层
#// =============================================================================
#//
#// 运行前置（在 Termux 中）：
#//   1. 安装：pkg install x11-repo termux-x11-nightly libx11 xorg-util-macros
#//   2. 启动 Termux-X11 应用（GitHub 安装 APK），然后执行：
#//        termux-x11 :0 &
#//   3. 设置显示：export DISPLAY=:0
#//   4. 运行：./vus run examples/guilite_termux_demo.vus
#//
#// 预期：手机屏幕经 Termux-X11 显示 360x240 窗口，含背景/点/线/矩形/填充/文字，
#//       任意按键或点击关闭按钮退出。
#// 注意：当前内置 8x8 位图字体仅含 ASCII，窗口标题可为中文（X11 层支持），
#//       但画面上的文字建议用英文，避免中文显示为空白。

打印("Termux X11 实机 GUI 测试开始\n")

# 初始化窗口（宽 360，高 240）
r = 图形_初始化(360, 240, "VUS GuiLite @ Termux")
打印("初始化=")
打印(r)
打印("\n")

# 整屏填充深色背景
r = 图形_填充(0, 0, 360, 240, 0x202020)
打印("背景=")
打印(r)
打印("\n")

# 画点（红色，3 个）
r = 图形_画点(20, 20, 0xFF0000)
r = 图形_画点(30, 20, 0xFF0000)
r = 图形_画点(40, 20, 0xFF0000)
打印("画点=")
打印(r)
打印("\n")

# 画线（绿色，横线与斜线）
r = 图形_画线(20, 40, 340, 40, 0x00FF00)
r = 图形_画线(340, 40, 20, 80, 0x00FF00)
打印("画线=")
打印(r)
打印("\n")

# 空心矩形（蓝色）
r = 图形_矩形(20, 100, 120, 60, 0x0000FF)
打印("矩形=")
打印(r)
打印("\n")

# 实心矩形（黄色）
r = 图形_填充(160, 100, 120, 60, 0xFFFF00)
打印("填充=")
打印(r)
打印("\n")

# 文字（青色 / 白色，用英文）
r = 图形_文字(20, 180, "VUS GUI Test", 0x00FFFF)
打印("文字=")
打印(r)
打印("\n")

r = 图形_文字(20, 200, "Press any key to exit", 0xFFFFFF)
打印("文字2=")
打印(r)
打印("\n")

# 刷新：把帧缓冲送到 X11 窗口
r = 图形_刷新()
打印("刷新=")
打印(r)
打印("\n")

# 保持：进入 X11 事件循环，按键或关闭窗口退出
r = 图形_保持()
打印("保持=")
打印(r)
打印("\n")

打印("Termux X11 实机 GUI 测试结束\n")