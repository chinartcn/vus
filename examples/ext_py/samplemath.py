# -*- coding: utf-8 -*-
"""运行期外部桥示例：Python 域模块（声明式导出）。"""

__vus_export__ = {
    "函数": ["计算面积", "合并数据"],
    "变量": ["计数", "配置"],
    "只读": ["版本"],
}

版本 = "1.0.0"
计数 = 0
配置 = {"倍率": 2, "标签": "示例"}


def __vus_init__(api, 参数):
    """初始化钩子：接收桥 API 与 导入外部 的 参数 字典（深拷贝）。"""
    global 计数
    计数 = 参数.get("基准", 0)
    return 0


def 计算面积(宽, 高):
    global 计数
    计数 += 1
    return 宽 * 高


def 合并数据(a, b):
    return a + b