#// 真 await 测试：等待协程完成并取回其返回值
定义 计算任务(参数):
    睡眠(10)          # 模拟异步耗时
    返回 参数 + 100

c = 协程(计算任务, 5)
r = 等待(c)
打印(f"await 结果: {r}\n")

# 带让出的协程：await 需多次驱动越过让出点
定义 分段任务(参数):
    返回 参数 * 2

c2 = 协程(分段任务, 21)
r2 = 等待(c2)
打印(f"await 结果2: {r2}\n")
print("=== await 测试完成 ===\n")