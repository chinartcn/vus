#// test_r6_forward_chain.vus — R6 二期回归：身份转发链 + 新建实参 → UAF
#// 触发路径：身份(x) 直接返回参数 x；转发(x) 返回 身份(x)。调用点
#//   转发(新建字符串) 的 R6「补还出生份」收割曾把返回槽共享的同一对象
#//   提前 free（heap-use-after-free，ASAN 命中 vus_ref ← vus_var_set）。
#// 修复：普通用户调用路径叠加运行时指针身份判定（返回值 != 实参才收割）。
#// 本测试高频循环验证转发结果不被破坏、不崩溃。
定义 身份(x):
    返回 x

定义 转发(x):
    返回 身份(x)

循环 i 从 1 到 200:
    结果 = 转发("值-" .. 转文本(i))
    噪音1 = 转文本(i * 7)
    噪音2 = "缓冲" .. 噪音1
    断言(结果 == "值-" .. 转文本(i), "转发链结果被破坏: " .. 结果)

打印("[R6 转发链] 通过\n")
