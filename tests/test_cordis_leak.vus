# 泄漏复现：循环内 发射事件 传计算字符串（NEW 出生引用应被 RAII 收割）
定义 观察者(_参数):
    打印("")

监听事件("更新", 观察者, "观察")

# 每次迭代产生一个 NEW VusString（算术表达式 vus_to_string 出生 ref=1），
# 若漏收割则 RSS 线性增长直至 OOM（修复前 1000 万次即可吃到数 GB）。
# 修复后内存稳定，循环完成并打印 ok。
循环 i 从 1 到 10000000:
    发射事件("更新", i + 1)

打印("leak_test ok")
