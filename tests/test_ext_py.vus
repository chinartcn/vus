# 运行期外部桥（FFI Bridge）函数互调正向测试
# Python 域 samplemath：计算面积/合并数据（int/float/list、None 与边界在 vars 用例补）
导入外部 (m, {源: "ext_py.samplemath", 参数: {基准: 10}})

断言(m.计算面积(3, 4) == "12", "整数参数×返回值")
断言(m.计算面积(2.5, 4) == "10", "浮点参数（4.0 舍入为 10）")
断言(m.计算面积(1.5, 2.5) == "3.75", "浮点返回精度（1.5×2.5=3.75）")

# 列表参数与返回值（深拷贝语义）+ 下标取值
_a = [1, 2]
_b = [3, 4]
合并 = m.合并数据(_a, _b)
断言(长度(合并) == "4", "列表合并长度")
断言(合并[0] == "1", "列表元素 0")
断言(合并[3] == "4", "列表元素 3")

# 惰性：仅声明未使用的别名不产生运行期开销（另开文件验证）——此处声明的 m 已使用

打印("test_ext_py ok")