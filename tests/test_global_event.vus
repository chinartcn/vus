// test_global_event.vus — 全局变量机制验证（普通函数场景）
全局计数 = 0

定义 增加():
    全局计数 = 全局计数 + 1

定义 主():
    打印("初始: ")
    打印(全局计数)
    打印("\n")
    增加()
    增加()
    打印("两次自增后: ")
    打印(全局计数)
    打印("\n")
    全局计数 = 100
    打印("重设后: ")
    打印(全局计数)
    打印("\n")

主()