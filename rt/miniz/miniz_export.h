/*
 * miniz_export.h — miniz 导出宏头（VUS vendor 占位）。
 * miniz.h 无条件 include 本文件，3.0.2 release tarball 未携带。
 * VUS 静态链入（libvus_rt.a），无需符号导出，MINIZ_EXPORT 留空即可。
 */
#pragma once

#ifndef MINIZ_EXPORT
#define MINIZ_EXPORT
#endif