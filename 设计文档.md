# VUS 语言设计文档 v0.1（最终修订·完整版）

> **状态**：✅ 设计定稿，可直接交付编译器开发团队落地实现  
> **口号**：中西合璧，天下无敌  
> **协议**：MIT  
> **核心原则**：让不懂英文的人也能写代码，同时保留对底层硬件的完全控制权  
> **最后更新**：2026 年 7 月 21 日


## 一、总览

### 1.1 语言定位

VUS 是面向 Linux、Android Termux、嵌入式 ARM 设备的中文友好多范式编译型强类型编程语言。

**定位**：填补 C 与 Python 之间的空白，不试图替代二者，而是做它们之间的桥梁：

| 用户类型 | 需求 | VUS 的应对方式 |
|----------|------|----------------|
| 完全不懂编程的中文用户 | 想写个小工具但被英文界面劝退 | 易语言风格（`.功能`）或函数风格中文别名 |
| 会一点 Python 的初学者 | 想写点能跑的东西，不想折腾环境 | 函数风格 + `vus run` 一键运行 |
| 有经验的开发者 | 需要控制底层但又不想写 C/汇编 | 强类型 + 编译到 C + 可选手动内存管理（v1.0） |
| 嵌入式/ARM 开发者 | 需要原生二进制，没有解释器依赖 | 编译到 C → ARM 机器码，无运行时（不含 Python） |
| 终端工具作者 | 想快速做出交互式界面但不想写 HTML | TUI 终端界面 + 扩展库 |

### 1.2 核心技术栈

```
VUS 源码
    ↓
【词法分析器】→ Token 流（含行号/列号）
    ↓
【语法分析器】→ AST（抽象语法树）
    ↓
【语义分析】→ 类型检查 / 符号表 / 循环依赖检测
    ↓
【C 代码生成器】→ .c 文件（含引用计数插入、闭包生成）
    ↓
【GCC/Clang 交叉编译】→ ARM/x86 原生可执行文件
```

**配套底层依赖**：纯 C 编写的 `libvus_rt` 运行时库，提供引用计数、字符串、列表、错误处理基础接口。Python、TUI、网络等功能均为**可选插件**，不侵入核心编译链路。

### 1.3 版本规划

| 版本 | 范围 | 预计周期 |
|------|------|----------|
| **v0.1 PoC** | 最小可验证版本：函数风格中文别名 + 打印/四则运算 + 基础运行时 | 3 个月 |
| **v0.1 正式版** | 双语法完整支持 + 流程控制 + 函数/闭包 + 错误处理 + 工具链 | 6-9 个月 |
| **v0.2** | 中文拼接运算符别名 `并`、调试栈回溯完善、报错体验优化 | 迭代增量 |
| **v1.0** | 手动内存管理、泛型、结构体、多线程、异步、官方包仓库、自举 | 12 个月+ |


## 二、语法风格（两种，项目级锁定）

VUS 支持两种语法风格，**项目创建时选择，之后不可切换**。风格在 `vus.json` 中静态锁定。

### 2.1 函数风格（推荐给熟悉 Python 的用户）

关键字提供 **英文 + 中文别名**，用户可混用或纯中文。

**英文写法**：

```
def 加法(a, b):
    return a + b

if 分数 >= 90:
    等级 = "优秀"
elif 分数 >= 60:
    等级 = "及格"
else:
    等级 = "不及格"

for i in range(1, 10):
    print(i)
```

**中文别名写法**（关键字全中文，其他语法不变）：

```
定义 加法(a, b):
    返回 a + b

如果 分数 >= 90:
    等级 = "优秀"
否则如果 分数 >= 60:
    等级 = "及格"
否则:
    等级 = "不及格"

循环 i 从 1 到 10:
    打印(i)
```

**中文别名完整对照表**：

| 英文关键字 | 中文别名 | 英文关键字 | 中文别名 |
|------------|----------|------------|----------|
| `def` | `定义` | `true` | `真` |
| `if` | `如果` | `false` | `假` |
| `elif` | `否则如果` | `null` | `空` |
| `else` | `否则` | `and` | `和` |
| `for` | `循环` | `or` | `或` |
| `while` | `当循环` | `not` | `非` |
| `return` | `返回` | `import` | `导入` |
| `try` | `尝试` | `from` | `从` |
| `except` | `捕获` | `global` | `全局` |

**空格规则（严格模式）**：

中文关键字与变量/表达式之间**必须有空格或换行分隔**，否则报错 **E015**。

| 示例 | 判定 |
|------|------|
| `循环 i 从 1 到 10` | ✅ 正确 |
| `循环i从1到10` | ❌ E015：关键字「循环」与变量「i」之间缺少空格 |
| `如果 分数 >= 90` | ✅ 正确 |
| `如果分数 >= 90` | ❌ E015：关键字「如果」与变量「分数」之间缺少空格 |

> **注**：英文关键字（`if`、`for`、`def` 等）不受此规则约束，遵循通用 C/Python 词法。运算符、括号、逗号、引号两侧无强制空格要求。

### 2.2 易语言风格

适合完全不想看到任何英文的用户，关键字全中文，必须带 `.` 前缀。

```
.功能 加法(a, b)
    .返回 a + b
.结束

.如果 分数 >= 90
    .等级 = "优秀"
.否则 分数 >= 60
    .等级 = "及格"
.否则
    .等级 = "不及格"
.结束

.计次循环 i, 1, 10
    .打印(i)
.结束
```

**关键字规则**：

1. 所有流程、函数关键字强制带 `.` 前缀：`.功能`、`.如果`、`.计次循环`、`.尝试`、`.捕获`、`.返回`、`.结束`。
2. 布尔常量 `真`/`假`、空值 `空` **不带点前缀**，为全局内置关键字，变量禁止同名。
3. 普通变量、函数调用无 `.` 前缀，天然与关键字隔离，无词法歧义。
4. 代码块必须以 `.结束` 闭合，缺少闭合块触发 **E001**。

> **错误捕获规则**：在易语言风格中，点号（`.`）后紧跟 `真`、`假`、`空` 三个内置常量关键字时，词法分析器**直接抛出 E001 语法错误**，并给出修复建议："布尔常量/空值无需点号前缀，请直接使用 `真`/`假`/`空`"。

> **注意**：易语言风格中的 `.循环` 对应函数风格的 `当循环`（`while`），而非函数风格中的 `循环`（`for`）。两种风格的"循环"关键字指向不同的控制流结构，请勿混用理解。

### 2.3 风格锁定与校验规则

- 项目创建时通过 `vus init` 交互式选择风格，结果写入 `vus.json` 的 `"风格"` 字段。
- 同一项目内所有 `.vus` 文件必须使用相同风格。
- 编译器语义分析阶段校验风格匹配：

| 配置风格 | 源码中出现 | 结果 |
|----------|-----------|------|
| `"函数"` | `.功能`、`.如果` 等点前缀关键字 | ❌ **E013**：风格不匹配 |
| `"易语言"` | `def`、`定义`、`if` 等无点关键字 | ❌ **E013**：风格不匹配 |

### 2.4 `vus init` 交互流程

```
==== VUS 项目初始化向导 v0.1 ====
本向导将为你的项目配置基础代码风格，选定后全局锁定。

【适配推荐】
  1. 函数风格：熟悉 Python、有编程基础，可中英混写
  2. 易语言风格：零基础，完全不想使用任何英文符号

【风格示例展示】
  [1] 函数风格
      定义 求和(a, b):
          返回 a + b
      打印(求和(1, 2))

  [2] 易语言风格
      .功能 求和(a, b)
          .返回 a + b
      .结束
      .打印(.求和(1, 2))

请输入选择编号 [1/2]：
> 1

已选择「函数风格」，确认保存该配置？(y/n)
> y

配置写入 vus.json 完成。
如需修改风格：
  1. 手动编辑项目根目录 vus.json 修改「风格」字段
  2. 执行 vus init --force 重新运行配置向导
```


## 三、类型系统（统一强类型）

VUS 使用**统一的强类型系统**，运算符行为在所有风格下完全一致。

### 3.1 基本类型

| 类型 | 内存规格 | 底层 C 映射 | 示例 |
|------|----------|-------------|------|
| 整型 | 64 位有符号 | `long long` | `a = 10` |
| 浮点型 | 64 位双精度 | `double` | `b = 3.14` |
| 字符串 | UTF-8，不可变 | `VusString*` | `c = "你好"` |
| 布尔型 | 固定常量 | `int` (0/1) | `d = 真` |
| 列表 | 动态数组，带类型标签 | `VusList*` | `e = [1, 2, 3]` |
| 字典 | 键值哈希表，键仅支持字符串 | `VusDict*` | `f = {"键": "值"}` |
| 空值 | 空指针标记 | `NULL` | `g = 空` |

**字符串字面量转义规则**：

字符串字面量支持以下转义序列：
- `\n` — 换行符（LF）
- `\r` — 回车符（CR）
- `\t` — 水平制表符
- `\\` — 反斜杠
- `\"` — 双引号（用于在字符串内嵌入引号）
- `\xHH` — 十六进制字节（如 `\xE4`）
- `\uHHHH` — Unicode 码点（如 `\u4F60` 表示"你"）

**不支持**八进制转义（`\0`、`\123` 等），使用时报错 **E001**。

### 3.2 运算符行为（固定，不随风格变化）

| 运算符 | 含义 | 约束 | 示例 |
|--------|------|------|--------|
| `+` | 数学加法 | 仅数字 | `3 + 5` → `8` |
| `-` | 数学减法 | 仅数字 | `10 - 3` → `7` |
| `*` | 数学乘法 | 仅数字 | `4 * 5` → `20` |
| `/` | 数学除法 | 仅数字 | `15 / 3` → `5` |
| `%` | 取模 | 仅数字 | `7 % 3` → `1` |
| `..` | 字符串拼接 | 仅字符串 | `"你好" .. "世界"` → `"你好世界"` |
| `==` `!=` | 相等/不等比较 | 同类型可比较 | `a == b` |
| `<` `>` `<=` `>=` | 大小比较 | 同类型可比较 | `a < b` |
| `&` | 按位与 | 仅数字 | `0b1100 & 0b1010` → `0b1000` |
| `\|` | 按位或 | 仅数字 | `0b1100 \| 0b1010` → `0b1110` |
| `^` | 按位异或 | 仅数字 | `0b1100 ^ 0b1010` → `0b0110` |
| `<<` `>>` | 左移/右移 | 仅数字 | `a << 2` |
| `和`/`and` | 逻辑与 | 仅布尔值 | `a 和 b` |
| `或`/`or` | 逻辑或 | 仅布尔值 | `a 或 b` |
| `非`/`not` | 逻辑非 | 仅布尔值 | `非 a` |

**类型错误规则**：

| 错误写法 | 结果 |
|----------|------|
| `数字 + 字符串` | ❌ **E002**：`+` 不支持字符串类型 |
| `数字 .. 数字` | ❌ **E002**：`..` 不支持数字类型 |
| `字符串 & 字符串` | ❌ **E002**：`&` 不支持字符串类型 |
| `数字 == 字符串` | ❌ **E002**：跨类型比较 |

### 3.3 列表类型一致性

**严格模式（默认，嵌入式推荐）**：

列表内所有元素必须类型一致，否则编译报错：

```
列表 = [1, 2, 3]         // ✅ 正确，全是整数
列表 = ["a", "b", "c"]   // ✅ 正确，全是字符串
列表 = [1, "hello", 3]   // ❌ E002：列表元素类型不一致（int vs str）
```

**混合模式（脚本工具推荐）**：

开启方式（二选一）：
1. 全局配置：`vus.json` 中设置 `"列表模式": "混合"`
2. 局部声明：安装 `vus-mixed-list` 插件后使用 `允许混合列表` 修饰列表

混合模式规则：
- `VusList->type` 固定为 `TYPE_MIXED`
- 每个堆元素附带独立类型标签，`vus_list_get` 不做类型校验
- **性能代价**：内存占用提升约 30%，遍历速度下降约 20%

**模式传递机制**：编译器在生成 C 代码时，将 `vus.json` 中的 `列表模式` 值作为宏定义（`VUS_LIST_MODE_STRICT` 或 `VUS_LIST_MODE_MIXED`）注入到生成的 `main.c` 中。运行时库根据该宏编译出不同行为。**严禁**在运行时动态切换列表模式。

### 3.4 类型推断与注解

```
// 自动推断
a = 10          // 推断为 int
名字 = "张三"    // 推断为 str

// 显式注解
b: int = 20
c: str = "hello"
d: list[int] = [1, 2, 3]
e: dict[str, int] = {"a": 1, "b": 2}
```

**类型冲突规则**：注解类型与推导类型不一致时抛出 **E002**。


## 四、变量与作用域

### 4.1 变量声明

**函数风格**：

```
a = 10          // 创建变量，自动推断类型
名字 = "张三"
b: int = 20     // 显式类型注解
```

**易语言风格**：

```
.a = 10
.名字 = "张三"
.b: int = 20
```

**命名约束**：
- 变量名可包含中文、英文、数字、下划线，但**不能以数字开头**
- 禁止与关键字同名（`真`、`假`、`空`、`定义`、`如果` 等），否则触发 **E003**
- 易语言风格变量无 `.` 前缀，关键字强制带 `.`，天然规避命名冲突

### 4.2 作用域规则

- 变量无需提前声明，直接赋值即创建
- 文件顶层变量为全局变量，在当前文件内可见
- 函数内部默认是局部变量，访问外层变量需显式声明 `全局`
- **循环/条件块无独立块级作用域**，变量生命周期延伸至函数末尾
- 跨文件访问需通过 `导入` 机制

**函数风格**：

```
全局变量 = 100

def 测试():
    局部变量 = 20
    全局 全局变量   // 声明要修改外层变量
    全局变量 = 200
```

**易语言风格**：

```
.全局变量 = 100

.功能 测试()
    .局部变量 = 20
    .全局 .全局变量
    .全局变量 = 200
.结束
```


## 五、函数与流程控制

### 5.1 函数定义

**函数风格（英文）**：

```
def 加法(a, b):
    return a + b

def 问候(名字="世界"):   // 默认参数
    打印("你好，" .. 名字)
```

**函数风格（中文别名）**：

```
定义 加法(a, b):
    返回 a + b

定义 问候(名字="世界"):
    打印("你好，" .. 名字)
```

**易语言风格**：

```
.功能 加法(a, b)
    .返回 a + b
.结束

.功能 问候(名字="世界")
    .打印("你好，" .. 名字)
.结束
```

### 5.2 条件判断

**函数风格（英文）**：

```
if 分数 >= 90:
    等级 = "优秀"
elif 分数 >= 60:
    等级 = "及格"
else:
    等级 = "不及格"
```

**函数风格（中文别名）**：

```
如果 分数 >= 90:
    等级 = "优秀"
否则如果 分数 >= 60:
    等级 = "及格"
否则:
    等级 = "不及格"
```

**易语言风格**：

```
.如果 分数 >= 90
    .等级 = "优秀"
.否则 分数 >= 60
    .等级 = "及格"
.否则
    .等级 = "不及格"
.结束
```

### 5.3 循环

**函数风格（英文）**：

```
for i in range(1, 11):
    打印(i)

while 计数 < 10:
    打印(计数)
    计数 = 计数 + 1

for 元素 in 列表:
    打印(元素)
```

**函数风格（中文别名）**：

```
循环 i 从 1 到 10:
    打印(i)

当循环 计数 < 10:
    打印(计数)
    计数 = 计数 + 1

循环 元素 在 列表:
    打印(元素)
```

**词法区分规则**：
- 若 `循环` 关键字后紧跟 `变量 从 数字 到 数字` 模式 → 解析为数值 `for` 循环
- 若 `循环` 关键字后紧跟 `变量 在 表达式` 模式 → 解析为 `foreach` 列表遍历
- 若两种模式都不匹配，抛出 **E001** 语法错误

**易语言风格**：

```
.计次循环 i, 1, 10
    .打印(i)
.结束

.循环 计数 < 10
    .打印(计数)
    .计数 = .计数 + 1
.结束

.遍历 列表 元素
    .打印(元素)
.结束
```

### 5.4 异常处理

**函数风格（英文）**：

```
try:
    结果 = 可能出错的操作()
except ValueError:
    打印("数值错误")
except:
    打印("未知错误")
    抛出("重新抛出")
```

**函数风格（中文别名）**：

```
尝试:
    结果 = 可能出错的操作()
捕获 ValueError:
    打印("数值错误")
捕获:
    打印("未知错误")
    抛出("重新抛出")
```

**易语言风格**：

```
.尝试
    .结果 = .可能出错的操作()
.捕获 ValueError
    .打印("数值错误")
.捕获
    .打印("未知错误")
    .抛出("重新抛出")
.结束
```

**底层实现说明**：采用 **Go 风格错误码链**，禁用 `setjmp`/`longjmp`，确保引用计数安全、资源正确释放。

**变量可见性规则**：`捕获` 块中只能访问 `尝试` 块**之前**声明的变量。`尝试` 块内声明的变量在 `捕获` 块中不可见，强制用户将可能失败的操作与变量声明分离。

### 5.5 函数作为值（闭包）

**函数风格**：

```
def 加(a, b):
    return a + b

def 执行(函数, a, b):
    return 函数(a, b)

结果 = 执行(加, 3, 5)    // 8
```

**易语言风格**（支持相同机制）：

```
.功能 加(a, b)
    .返回 a + b
.结束

.功能 执行(函数, a, b)
    .返回 函数(a, b)
.结束

.结果 = .执行(.加, 3, 5)    // 8
```

**底层实现**：C 层翻译为 `struct VusClosure { void* (*func)(void*, ...); void* env; }`，`env` 指向堆分配的上下文结构体，由引用计数管理生命周期。

**高阶函数类型校验（v0.1 固定决策）**：

- 回调函数统一类型签名：`(任意类型) → 空`
- C 底层映射：`void (*callback)(void*)`
- 仅校验参数个数，参数数量不匹配时抛出 **E004**
- 参数类型不匹配编译无报错，运行时报错
- v1.0 将引入泛型完善类型校验

**`遍历列表` 回调调用规则**：
1. 回调函数必须接受 **恰好 1 个参数**，否则编译报错 **E004**。
2. 回调函数的参数类型**不做编译时校验**（v0.1 暂不支持泛型）。如果类型不匹配，运行时会抛出错误并终止程序。
3. 回调函数的返回值被忽略（如果回调有返回值，编译器生成 `(void)callback(item)` 丢弃）。
4. 回调函数由 `遍历列表` 的 C 实现逐元素调用，每次传入 `void* item`。


## 六、终端交互

### 6.1 基础交互（内置，依赖 libc）

**函数风格**：

```
打印("欢迎使用VUS")
名字 = 输入("请输入你的名字：")
打印("你好，" .. 名字)
```

**易语言风格**：

```
.打印("欢迎使用VUS")
.名字 = .输入("请输入你的名字：")
.打印("你好，" .. 名字)
```

底层使用 `printf` + `scanf`，包含在核心库中。

### 6.2 进阶 TUI（可选，需额外安装）

通过 `vus install tui` 安装 TUI 扩展，提供 `ncurses` 支持。

```
// 函数风格（需安装 vus-tui）
菜单 = 创建菜单(标题="请选择操作")
菜单.添加选项("1. 计算器")
菜单.添加选项("2. 待办清单")
选择 = 菜单.显示()
```

### 6.3 交互模式

| 模式 | 说明 | 依赖 |
|------|------|------|
| 问答式 | `打印()` / `输入()` 逐行交互 | 内置 |
| 菜单式 | 方向键选择列表项 | `vus-tui` 插件 |
| 编辑式 | 可编辑文本区域（类似 `nano`） | `vus-tui` 插件 |
| 游戏式 | 全屏刷新，光标自由移动 | `vus-tui` 插件 |


## 七、包管理

### 7.1 命令

```
vus create 包名                 // 创建包
vus add 包名                    // 添加依赖（自动下载并写入 vus.json 的「依赖」字段）
vus publish 包名 --repo=仓库名   // 发布到仓库（v0.1 实验性功能，仅支持本地路径）
```

> **`vus add` 行为**：自动从官方仓库下载包到 `.vus_packages/`，并将包名和版本写入 `vus.json` 的 `"依赖"` 字段。如果包已存在，升级到最新版本并更新 `vus.json`。

> **`vus publish` 说明**：v0.1 中为实验性功能，仅支持本地文件系统路径作为仓库地址（`--repo=./my_local_repo/`）。远程仓库认证功能推迟到 v1.0。

### 7.2 导入语法

**函数风格**：

```
import "工具.vus"
from "工具.vus" import 计算
from "工具.vus" import *
```

**函数风格（中文别名）**：

```
导入 "工具.vus"
从 "工具.vus" 导入 计算
从 "工具.vus" 导入 *
```

**易语言风格**：

```
.导入 "工具.vus"
.从 "工具.vus" 导入 .计算
.从 "工具.vus" 导入 *
```

**导入约束**：
- 仅支持同项目内相对路径导入，不支持绝对系统路径
- 通配符导入（`import *`）仅引入顶层全局函数/变量，不导入私有内部标识

### 7.3 循环依赖处理

**策略**：**禁止循环依赖**，编译器检测到循环引用时直接报错 **E009**。

**检测时机**：符号表构建阶段（语义分析前置步骤）

**检测逻辑**：深度遍历导入依赖链，记录已加载模块栈，若导入栈重复出现模块名则判定循环引用。

**报错示例**：

```
E009 模块循环依赖
依赖链：main.vus → 工具.vus → main.vus
解决方案：拆分公共逻辑至独立公共模块，消除双向导入
```

### 7.4 项目配置（`vus.json`）

```json
{
    "name": "我的工具",
    "version": "0.1.0",
    "作者": "神秘人",
    "描述": "一个用VUS写的终端工具",
    "风格": "函数",
    "主文件": "main.vus",
    "输出模式": "c",
    "列表模式": "严格",
    "调试": true,
    "目标平台": "linux-gnu",
    "依赖": [],
    "python": {
        "启用": false,
        "嵌入式": false,
        "版本": "3.10"
    },
    "编译选项": {
        "优化": "速度",
        "ARM版本": "ARM64",
        "警告等级": "全部"
    }
}
```

**字段约束**：

| 字段 | 合法值 | 非法值处理 |
|------|--------|------------|
| `"风格"` | `"函数"` / `"易语言"` | ❌ **E014** |
| `"列表模式"` | `"严格"` / `"混合"` | ❌ **E014** |
| `"输出模式"` | `"c"` / `"exe"` | ❌ **E014** |
| `"调试"` | `true` / `false` | ❌ **E014** |
| `"目标平台"` | `"linux-gnu"` / `"linux-musl"` / `"android"` | ❌ **E014** |
| `"python.启用"` | `true` / `false` | ❌ **E014** |
| `"python.版本"` | 字符串，如 `"3.10"`、`"3.11"` | ❌ **E014** |

> **配置与代码风格冲突**：如配置为 `"函数"` 但源码出现 `.功能`，抛出 **E013**。

**目标平台说明**：
- `"linux-gnu"`（默认）：标准 glibc 环境
- `"linux-musl"`：轻量 musl libc 环境（适合 Alpine Linux）
- `"android"`：Android Termux 环境（bionic libc）

### 7.5 项目目录结构

```
我的项目/
├── vus.json
├── main.vus
├── libs/
│   ├── 工具.vus
│   └── 配置.vus
├── 资源/
│   └── config.json
├── 测试/
│   └── test_工具.vus
├── 构建/
│   └── main.c
└── .vus_cache/          // 编译缓存目录（自动生成）
```


## 八、编译与构建

### 8.1 编译流程

```
.vus 源文件
    ↓
【词法分析器】→ 加载风格配置，切换双模式分词逻辑，校验空格规则（E015）
    ↓
【语法分析器】→ 生成 AST（含行号/列号位置信息）
    ↓
【语义分析】→ 符号表构建 / 类型检查（E002）/ 循环依赖检测（E009）/ 风格匹配校验（E013）
    ↓
【C 代码生成器】→ 自动插入引用计数操作 / 生成闭包 env 结构体 / 预埋调试行号信息
    ↓
【可选】调用 GCC/Clang → ARM 可执行文件
```

### 8.2 引用计数插入规则（代码生成强制规范）

编译器在生成 C 代码时，按以下规则自动插入引用计数操作：

| 场景 | 生成代码 | 说明 |
|------|----------|------|
| **变量赋值 `a = b`** | `vus_ref(b); vus_unref(a); a = b;` | 若 `a` 未初始化，跳过 `unref` |
| **函数参数传递** | 形参持有实参，自动 `vus_ref(实参)` | 实参本身引用计数不变 |
| **函数返回值** | 返回值作为临时对象压栈，调用方接收后 `vus_ref(返回值)`，随后 `vus_unref(临时返回值)` | 释放栈上临时引用 |
| **列表添加元素** | `vus_list_append` 内部自动 `vus_ref(item)` | 列表销毁时遍历所有元素 `vus_unref` |
| **数字/布尔字面量** | **不生成**引用计数代码 | 立即数直接栈分配 |

### 8.3 编译命令

```
vus init                    // 首次安装配置（交互式选择风格）
vus init --force            // 强制重新配置现有项目风格
vus build                   // 根据 vus.json 生成 .c 文件
vus build --c-only          // 只生成 .c，不调用 GCC
vus build --exe             // 生成 .c + 调用 GCC 生成可执行文件
vus build --rebuild         // 强制全量重编（清空缓存）
vus run                     // 生成 .c → GCC → 运行
vus test                    // 运行测试（详见 8.4）
vus test --parallel         // 并行执行测试（v1.0 规划，v0.1 暂不支持）
```

**缓存策略**：`.vus_cache/` 目录存储编译中间文件。`vus build` 基于源文件修改时间判断是否需要重新编译。`vus.json` 配置变更时自动清空缓存。如需强制全量重编，使用 `vus build --rebuild`。

### 8.4 `vus test` 详细规范

1. **文件发现**：递归扫描项目根目录下 `测试/` 文件夹中所有 **`test_*.vus`** 文件。
2. **执行顺序**：按文件名升序依次编译并执行（默认串行）。
3. **成功标准**：程序正常退出（`exit(0)`）且未触发 `断言(条件, 消息)` 失败。
4. **失败判定**：任何测试用例编译失败、运行时抛出未捕获异常、或断言失败，均视为整体测试失败，`vus test` 退出码为 `1`。
5. **输出汇总**：测试结束后输出总计用例数、通过数、失败数。

**输出格式示例**：
```
测试完成：共 3 个用例，通过 2 个，失败 1 个
失败用例：test_工具.vus (E002: 类型不匹配)
```

**并行执行**：默认串行执行。使用 `vus test --parallel` 启用并行测试（v0.1 暂不支持，v1.0 规划）。

### 8.5 输出模式

| 模式 | 说明 | 命令行 |
|------|------|--------|
| `c`（默认） | 只生成 `.c` 文件 | `vus build --c-only` |
| `exe` | 生成 `.c` 并调用 GCC | `vus build --exe` |

**GCC 调用参数**：`vus build --exe` 默认使用 `gcc -O2 -g -o <输出> <源文件> -lvus_rt`。`vus.json` 中 `编译选项.优化` 控制 `-O` 级别：

| `优化` 值 | GCC 参数 | 说明 |
|-----------|----------|------|
| `"速度"` | `-O2` | 默认，平衡性能与体积 |
| `"体积"` | `-Os` | 优化体积，适合嵌入式 |
| `"调试"` | `-O0 -g` | 关闭优化，保留调试符号 |

### 8.6 跨平台策略

- **主目标平台**：ARM32/ARM64 + Linux（含 Android Termux）
- **辅助方案**：用户可通过 Python 垫层在 Windows/macOS 上运行 VUS
- **交叉编译**：用户可自行指定 GCC 目标架构（如 `--arch=armv7`）

> **命令行参数优先级**：`--arch` 命令行参数会覆盖 `vus.json` 中的 `ARM版本` 配置。


## 九、Python 桥接（可选插件）

### 9.1 安装与启用

Python 嵌入**默认不启用**，需要用户主动安装插件：

```
vus install python-embed
```

安装后，在 `vus.json` 中配置：

```json
"python": {
    "启用": true,
    "嵌入式": true,
    "版本": "3.10"
}
```

> **⚠️ 重要警示**：启用 Python 嵌入会使二进制体积膨胀至 **15-20MB**，且依赖 libpython 动态库。仅推荐用于 PC 工具类应用。ARM 嵌入式场景官方文档明确**不推荐启用**。

**Python 版本锁定**：默认使用系统 `python3`。如需锁定特定版本，在 `vus.json` 的 `python.版本` 字段中指定（如 `"3.10"`、`"3.11"`）。静态打包时使用该版本对应的 libpython。

**嵌入式模式二选一**：

| 模式 | 说明 | 优点 | 缺点 |
|------|------|------|------|
| 动态链接 | 依赖系统 libpython | 体积更小 | 跨设备兼容性差 |
| 静态打包 | 内置完整 Python 解释器 | 单文件分发 | 体积大幅增加 |

### 9.2 内联 Python 代码

**函数风格**：

```
<python>
import requests
data = requests.get("https://api.example.com/data").json()
result = data["value"] * 2
</python>

打印(result)
```

**易语言风格**：

```
<.python>
import requests
data = requests.get("https://api.example.com/data").json()
result = data["value"] * 2
</.python>

.打印(.结果)
```

### 9.3 自动类型转换

| VUS 类型 | Python 类型 |
|----------|-------------|
| 整型 | int |
| 浮点型 | float |
| 字符串 | str |
| 布尔型 | bool |
| 列表 | list |
| 字典 | dict |
| 空值 | None |

VUS 变量在 `<python>` 块中自动可见，Python 变量在块结束后自动映射回 VUS 变量。转换失败时抛出 **E007**。

**变量覆盖规则**：`<python>` 块结束时，Python 中所有非内置名称自动导出回 VUS 变量，**覆盖**同名 VUS 变量。如果用户不希望覆盖，可在 Python 代码中使用 `__vus_skip_export__` 标记或采用不同变量名。

### 9.4 扩展包机制

```
包 图像处理 = vus.扩展包("PIL")   // 加载 Python Pillow 库
图像处理.调整大小("image.png", 宽=200, 高=200)
```


## 十、标准库

### 10.1 核心库（内置，依赖 libc）

**输出/输入**：

```
打印(内容)
输入("提示")
```

**字符串操作**：

```
长度(文本)
拼接(文本1, 文本2)   // 或 文本1 .. 文本2
分割(文本, 分隔符)
替换(文本, 旧, 新)
取子串(文本, 开始, 长度)
```

**数字处理**：

```
转数字(文本)          // 转换失败时抛出异常（E002），由 尝试/捕获 处理
转文本(数字)
取整(数字)
取随机数(最小值, 最大值)
```

> **`转数字` 行为**：将文本转换为整数。转换失败时抛出异常（触发 **E002**），由最近的 `尝试/捕获` 块捕获。如果不捕获，程序终止。底层实现通过 `int64_t vus_to_int(VusString* s, int* err)` 完成，`err` 为 0 表示成功，非 0 表示失败。

**列表操作**：

```
创建列表(元素...)
添加元素(列表, 元素)
取元素(列表, 索引)
删除元素(列表, 索引)
列表长度(列表)
遍历列表(列表, 函数)
```

**字典操作**：

```
创建字典()
字典设值(字典, 键, 值)   // 键仅支持字符串
字典取值(字典, 键)
字典删除(字典, 键)
字典长度(字典)
```

**文件操作**：

```
读取文件(路径)
写入文件(路径, 内容)
追加文件(路径, 内容)
删除文件(路径)
文件是否存在(路径)
```

**时间与调试**：

```
当前时间()
等待(毫秒)
调试输出(内容)
退出(退出码=0)          // 终止程序，默认返回 0（成功）
断言(条件, 消息)        // 仅调试模式有效
```

> **`断言` 行为规则**：
> 1. 当 `vus.json` 中 `"调试": true` 时，断言失败时输出消息并终止程序（退出码 1）。
> 2. 当 `"调试": false` 时，断言**被编译器移除**，不生成任何 C 代码（类似 C 的 `NDEBUG` 宏）。
> 3. 断言失败**不触发异常机制**（不经过 `尝试/捕获`），直接终止。

### 10.2 扩展库（需 `vus add`）

| 模块 | 功能 | 依赖 |
|------|------|------|
| `网络` | HTTP GET/POST | libcurl |
| `加密` | MD5/SHA256/AES | OpenSSL |
| `图片` | 缩放/旋转 | ImageMagick / STB |
| `数据库` | SQL 查询/执行 | SQLite |
| `json` | 解析/生成 | cJSON |
| `正则` | 匹配/替换 | PCRE |
| `数学` | 三角函数/对数 | libm |
| `tui` | 高级终端界面 | ncurses |


## 十一、错误处理

### 11.1 报错格式

编译器报错时提供精确位置和修复建议：

```
test.vus 第 2 行：创建按钮(文本="确定" 左边=5)
                              ↑
错误类型：语法错误
错误内容：参数之间缺少逗号
修复建议：在 "确定" 后面加一个逗号
参考示例：创建按钮(文本="确定", 左边=5)
```

### 11.2 错误码表

| 错误码 | 名称 | 触发场景 | 修复建议 |
|--------|------|----------|----------|
| **E001** | 语法格式错误 | 括号不匹配、缺少逗号、关键字拼写错误、易语言缺少 `.结束`、`.` 后误跟 `真`/`假`/`空`、字符串转义非法 | 检查括号配对，补齐逗号，修正关键字拼写，补齐代码块闭合标记 |
| **E002** | 类型不匹配 | 数字与字符串相加、列表元素类型混乱、运算符类型错误、`转数字` 转换失败 | 统一运算双方数据类型，混合列表修改配置，检查文本格式 |
| **E003** | 名称未定义/关键字冲突 | 使用未声明变量、变量命名与 `真`/`假`/`定义` 重名 | 修改变量名，提前定义标识符 |
| **E004** | 函数参数错误 | 参数数量不符、参数类型注解不匹配 | 核对函数定义与调用参数列表 |
| **E005** | 文件访问错误 | 导入文件不存在、读写文件路径无效 | 检查文件相对路径，确认文件存在 |
| **E006** | 内存管理错误 | 内存分配失败、手动内存 API（v0.1 禁用） | 减少超大对象创建，v0.1 禁止手动内存操作 |
| **E007** | Python 插件异常 | 内联 Python 代码报错、类型转换失败 | 调试 Python 代码，检查变量类型匹配规则 |
| **E008** | 包依赖错误 | 依赖包缺失、版本冲突 | 执行 `vus add` 安装对应依赖包 |
| **E009** | 模块循环依赖 | 文件双向互相导入 | 拆分公共逻辑至独立公共模块 |
| **E010** | 递归调用过深 | 函数递归层级超出运行时限制 | 改用循环迭代，减少递归深度 |
| **E011** | 操作超时 | 文件/网络操作超时 | 延长超时时间或拆分长耗时操作 |
| **E012** | 权限不足 | 文件读写、系统调用权限受限 | 修改文件权限，使用管理员权限运行 |
| **E013** | 代码风格不匹配 | 文件使用另一套语法关键字 | 修改代码适配项目锁定风格，或重新 `init` 配置 |
| **E014** | 配置错误 | `vus.json` 配置字段缺失、取值非法 | 对照标准配置模板修正 JSON 字段 |
| **E015** | 中文空格错误 | 中文关键字与标识符之间缺少空格/换行 | 在关键字与变量/表达式之间增加空格 |

### 11.3 调试模式调用链输出格式

当 `vus.json` 中 `"调试": true` 时，异常发生时输出完整调用链：

```
E007 Python插件异常 (代码行号: 15)
调用链 (最新调用在最上):
    main.vus:22 -> 计算器()
    工具.vus:8 -> 转数字()
    内部错误: ValueError: invalid literal for int()
```

**格式要素**：文件名、行号、函数名、错误消息缺一不可。


## 十二、内存管理（v0.1 只做自动管理）

VUS v0.1 采用**自动内存管理（引用计数 + 循环检测 GC）**，用户无需手动管理内存。

- 所有对象由编译器自动追踪生命周期
- 程序员不需要写 `申请内存` / `释放内存`
- 手动内存管理在 v0.1 中**不支持**，如果在代码中使用会报错（**E006**）
- v1.0 将引入手动内存管理作为高级特性


## 十三、版本兼容

VUS 提供迁移工具 `vus migrate` 帮助用户将旧版本代码升级到新版本。

```
vus migrate             // 分析当前项目，提示需要迁移的内容
vus migrate --apply     // 自动迁移（v0.1 为全量迁移，v0.2 将支持 --only 选择性应用）
vus migrate --manual    // 生成迁移报告，用户手动处理
```

**v0.1 迁移工具已知转换规则示例**：

| 旧语法 | 新语法 | 条件 |
|--------|--------|------|
| `&` 字符串拼接 | `..` 拼接 | 仅当左右操作数均为字符串且结果为字符串 |
| `vus.set.风格_` 动态切换语句 | 直接删除 | v0.1 已不支持动态风格切换 |
| 易语言风格旧式 `.如果` 缺少 `.结束` | 自动补全 `.结束` | 检测到未闭合的代码块 |
| 列表 `[1, "2"]` 在严格模式下报错 | 提示用户添加 `允许混合列表` 声明或修改配置 | 仅严格模式 |


## 十四、完整示例

### 函数风格（中文别名）版本

```
#// 这是一个完整的 VUS 示例程序（函数风格，中文别名）

// ============ 导入 ============
导入 "工具.vus"

// ============ 变量 ============
欢迎语 = "欢迎使用 VUS"
版本号 = 0.1

// ============ 函数 ============
定义 显示菜单():
    打印("=== " .. 欢迎语 .. " ===")
    打印("版本：" .. 转文本(版本号))
    打印("1. 计算器")
    打印("2. 待办清单")
    打印("3. 退出")
    返回 输入("请选择：")

定义 计算器():
    a = 转数字(输入("第一个数："))
    b = 转数字(输入("第二个数："))
    打印("结果：" .. 转文本(a + b))

定义 打印事项(事项文本):
    打印("- " .. 事项文本)

定义 待办清单():
    事项 = 创建列表()
    当循环 真:
        打印("\n当前清单：")
        遍历列表(事项, 打印事项)
        操作 = 输入("添加/删除/退出：")
        如果 操作 == "添加":
            添加元素(事项, 输入("事项内容："))
        否则如果 操作 == "删除":
            删除元素(事项, 转数字(输入("索引：")))
        否则:
            跳出

// ============ 主程序 ============
尝试:
    当循环 真:
        选择 = 显示菜单()
        如果 选择 == "1":
            计算器()
        否则如果 选择 == "2":
            待办清单()
        否则如果 选择 == "3":
            打印("再见！")
            退出()
        否则:
            打印("无效选择，请重新输入")
捕获 KeyboardInterrupt:
    打印("用户中断")
    退出()
捕获:
    打印("未知异常")
    退出()
```

### 易语言风格版本

```
#// 这是一个完整的 VUS 示例程序（易语言风格）

.导入 "工具.vus"

.欢迎语 = "欢迎使用 VUS"
.版本号 = 0.1

.功能 .显示菜单()
    .打印("=== " .. .欢迎语 .. " ===")
    .打印("版本：" .. .转文本(.版本号))
    .打印("1. 计算器")
    .打印("2. 待办清单")
    .打印("3. 退出")
    .返回 .输入("请选择：")
.结束

.功能 .计算器()
    .a = .转数字(.输入("第一个数："))
    .b = .转数字(.输入("第二个数："))
    .打印("结果：" .. .转文本(.a + .b))
.结束

.功能 .打印事项(.事项文本)
    .打印("- " .. .事项文本)
.结束

.功能 .待办清单()
    .事项 = .创建列表()
    .循环 .真
        .打印("\n当前清单：")
        .遍历 .事项 .打印事项
        .操作 = .输入("添加/删除/退出：")
        .如果 .操作 == "添加"
            .添加元素(.事项, .输入("事项内容："))
        .否则 .操作 == "删除"
            .删除元素(.事项, .转数字(.输入("索引：")))
        .否则
            .跳出
        .结束
    .结束
.结束

.尝试
    .循环 .真
        .选择 = .显示菜单()
        .如果 .选择 == "1"
            .计算器()
        .否则 .选择 == "2"
            .待办清单()
        .否则 .选择 == "3"
            .打印("再见！")
            .退出()
        .否则
            .打印("无效选择，请重新输入")
        .结束
    .结束
.捕获 KeyboardInterrupt
    .打印("用户中断")
    .退出()
.捕获
    .打印("未知异常")
    .退出()
.结束
```


## 十五、命令速查表

| 命令 | 作用 |
|------|------|
| `vus init` | 新项目交互式初始化，选择语法风格 |
| `vus init --force` | 强制重新配置现有项目风格，覆盖 `vus.json` |
| `vus build` | 读取配置，生成中间 C 源码 |
| `vus build --c-only` | 仅输出 C 文件，不调用编译器生成二进制 |
| `vus build --exe` | 编译 C 源码并调用 GCC 生成可执行程序 |
| `vus build --rebuild` | 强制全量重编（清空缓存） |
| `vus run` | 构建 + 运行生成的二进制文件 |
| `vus test` | 自动执行 `测试/` 目录下全部测试用例 |
| `vus test --parallel` | 并行执行测试（v1.0 规划，v0.1 暂不支持） |
| `vus migrate` | 扫描项目代码，输出版本迁移修改报告 |
| `vus migrate --apply` | 根据报告自动批量迁移旧语法代码 |
| `vus migrate --manual` | 仅生成迁移文档，不自动修改源码 |
| `vus create 包名` | 创建标准目录结构的 VUS 包项目 |
| `vus add 包名` | 添加第三方依赖包至 `vus.json` |
| `vus publish 包名 --repo=仓库地址` | 发布自定义包至本地仓库（v0.1 实验性） |
| `vus install 插件名` | 安装扩展插件（`python-embed`、`tui` 等） |


## 十六、设计决策记录

| 决策点 | 选择 | 核心理由 | 取舍代价/后续迭代 |
|--------|------|----------|-------------------|
| **双语法体系** | 函数风格中英别名 + 易语言点前缀 | 覆盖零基础与有编程基础两类用户 | 新增语法特性需同步适配两套风格，维护成本翻倍 |
| **风格锁定机制** | 项目全局配置锁定，不可单文件切换 | 简化词法分析状态机，消除语法歧义 | 项目中途切换风格需全局重构，提供 `vus migrate` |
| **中文别名空格规则** | 严格模式，必须有空格/换行分隔（E015） | 避免词法歧义（如 `循环i` vs `循环 i`） | 用户需适应中文输入法下的空格习惯 |
| **字符串拼接运算符** | `..` 为主，v0.2 新增中文别名 `并` | 对齐 Lua/C 主流运算符习惯，`&` 归还位运算 | 中文输入法切换标点输入繁琐，v0.2 提供纯中文操作符 |
| **布尔常量 `真`/`假`** | 全局顶层关键字，两套风格通用 | 避免易语言风格额外加点前缀，语法统一 | 用户无法创建同名变量，提前报错提示 |
| **高阶函数类型校验** | v0.1 仅校验参数个数，不校验元素类型 | 快速落地高阶遍历功能，降低编译器复杂度 | 牺牲部分强类型安全；v1.0 泛型完善完整类型校验 |
| **模块循环依赖** | 直接报错 E009，不支持延迟解析 | 对齐 C/Go/Rust 主流编译规则，实现简单稳定 | 项目分层设计要求更高，需拆分公共模块 |
| **异常底层实现** | Go 错误码链，禁用 `setjmp` | 保证自动 RC 内存安全，资源可完整释放 | 无原生栈回溯；调试模式预埋行号链弥补缺陷 |
| **闭包实现** | 结构体 + env 指针，引用计数管理 | 在 C 中安全实现闭包，生命周期可控 | 闭包捕获变量需堆分配，增加内存开销 |
| **编译目标** | VUS → C，复用 GCC 工具链 | 无需从零开发机器码后端，跨 ARM 平台简单 | 编译链路多一层转换，小幅增加编译耗时 |
| **Python 集成** | 可选插件，默认关闭 | 核心运行时轻量化，适配嵌入式场景 | 重度工具项目依赖 Python 生态，原生库发展缓慢 |
| **内存管理 v0.1** | 仅自动引用计数，无手动 API | 降低新手内存操作门槛 | 极致性能 MCU 设备存在 RC 开销；v1.0 开放手动内存管理 |
| **列表默认模式** | 严格强类型 | 运行时性能最优，规避大量运行时类型崩溃 | 动态脚本开发繁琐；提供混合模式配置开关 |
| **报错语言** | v0.1 仅中文报错 | 面向中文用户，符合语言定位 | 英文用户/国际社区需等待 v1.0 |
| **`转数字` 错误处理** | 转换失败抛出异常（E002） | 正统错误处理路径，用户可用 `尝试/捕获` 兜底 | 需要运行时错误链支持 |
| **字典键类型** | 仅支持字符串键 | 简化运行时实现，避免 int/str 键类型混合的复杂性 | 灵活性降低；v1.0 可考虑扩展 |
| **断言行为** | 调试模式有效，非调试模式编译移除 | 对齐 C 的 `assert` 行为，生产环境零开销 | 调试时需开启 `"调试": true` |


## 附录 A：运行时库（`libvus_rt`）完整 C 头文件

以下为 `libvus_rt.h` 完整规范，可直接交付 C 开发组实现：

```c
#ifndef VUS_RT_H
#define VUS_RT_H

#include <stdint.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>

// ============ 类型标记常量 ============
#define TYPE_INT     1
#define TYPE_FLOAT   2
#define TYPE_STR     3
#define TYPE_BOOL    4
#define TYPE_MIXED   99

// ============ 前向声明 ============
typedef struct VusString VusString;
typedef struct VusList VusList;
typedef struct VusDict VusDict;
typedef struct VusClosure VusClosure;
typedef struct VusError VusError;

// ============ 引用计数通用操作 ============
void vus_ref(void* obj);
void vus_unref(void* obj);

// ============ 字符串 ============
// data 始终以 '\0' 结尾（便于 C 互操作），但字符串内容可能包含中间 '\0'。
// len 表示有效字节长度（不含结尾 '\0'），保证 len <= strlen(data)。
struct VusString {
    int ref;
    int len;          // UTF-8 字节长度
    char* data;       // 只读字符缓冲区
};

VusString* vus_string_new(const char* s);
VusString* vus_string_new_len(const char* s, int len);
VusString* vus_string_concat(VusString* a, VusString* b);
VusString* vus_string_slice(VusString* s, int start, int len);
int vus_string_len(VusString* s);
char* vus_string_cstr(VusString* s);  // 返回的指针指向 data，调用方禁止修改或释放

// ============ 列表 ============
struct VusList {
    int ref;
    int len;
    int cap;
    void** items;
    int type;          // 元素类型标记（严格模式）/ TYPE_MIXED（混合模式）
};

VusList* vus_list_new(int type);
void vus_list_append(VusList* list, void* item);
void* vus_list_get(VusList* list, int index);
void vus_list_remove(VusList* list, int index);
void vus_list_set(VusList* list, int index, void* item);
int vus_list_len(VusList* list);

// ============ 字典 ============
// 键仅支持 VusString*（字符串），值可为任意 VUS 对象
// 哈希表实现策略：链地址法（separate chaining），负载因子超过 0.75 时自动扩容
// v0.1 不提供字典遍历接口，v1.0 补充
struct VusDict {
    int ref;
    void* impl;       // 哈希表实现（运行时库内部）
};

VusDict* vus_dict_new(void);
void vus_dict_set(VusDict* dict, VusString* key, void* value);
void* vus_dict_get(VusDict* dict, VusString* key);
void vus_dict_remove(VusDict* dict, VusString* key);
int vus_dict_len(VusDict* dict);

// ============ 闭包 ============
// 闭包参数约定：args 由调用方分配，调用结束后由调用方负责释放（vus_unref）。
// func 在调用期间持有 args 的引用，但不负责释放。
struct VusClosure {
    int ref;
    void (*func)(void* env, void* args);
    void* env;
};

VusClosure* vus_closure_new(void (*func)(void*, void*), void* env);
void vus_closure_call(VusClosure* closure, void* args);

// ============ 错误处理（错误码链） ============
// 注意：VusError 不参与引用计数，由运行时库独立管理，
// 用户无需调用 vus_ref/vus_unref。
struct VusError {
    int code;
    int line;
    const char* func;
    const char* msg;
    VusError* next;   // 错误链（最新错误在链头）
};

VusError* vus_error_new(int code, const char* msg, int line, const char* func);
void vus_error_push(VusError** chain, VusError* err);
void vus_error_print(VusError* err);
void vus_error_free(VusError* err);

// ============ 标准库辅助函数 ============
// 以下函数由编译器生成的 C 代码调用，用于标准库功能

void vus_print(VusString* s);
VusString* vus_input(VusString* prompt);

// vus_to_int：字符串转整数。成功时返回结果，*err 置 0。
// 失败时返回 0，*err 置非 0（调用方应检查 err 并抛出异常）。
int64_t vus_to_int(VusString* s, int* err);

VusString* vus_to_string(int64_t n);

// vus_to_float：字符串转浮点数。成功时返回结果，*err 置 0。
// 失败时返回 0.0，*err 置非 0。
double vus_to_float(VusString* s, int* err);

#endif // VUS_RT_H
```


**文档版本**：v0.1（最终修订·完整版）  
**状态**：✅ 设计定稿，可直接交付编译器开发团队落地实现  
**最后更新**：2026 年 7 月 21 日