// utils.vus — 通用工具逻辑库（随 .vaz 扩展包引入）
// 构建期由 `vus vaz expand` 合并输出，可被页面事件绑定调用。
// 注意: 函数名带前缀 _vaz_，避免与主逻辑重名冲突。

定义 _vaz_执行搜索(关键词):
    日志_级别(1, "搜索关键词=" + 关键词)
    如果 关键词 == "":
        返回 0
    结束
    返回 1
结束

定义 _vaz_拼接页脚(标题):
    返回 "— " + 标题 + " —"
结束

定义 _vaz_星级文本(值):
    如果 转数字(值) <= 0:
        返回 "未评分"
    结束
    返回 转文本(值) + "/5 星"
结束