> 文档版本：v1.0_apk（APK 功能时代）
> 最后更新时间：2026-09-04


# VUS 性能优化专项记录

> 版本：v3.0.20260904150204（正式版）
> 范围：编译器速度、生成代码质量、运行时核心库 `libvus_rt` 性能。
> 基准工具位于 `.bench/`（`big.vus` 大文件编译基准、`hot.vus`/`hot2.vus` 热循环运行基准）。

## 一、优化总览与收益

| 指标 | 优化前 | 优化后 | 提升 |
|------|-------:|-------:|------|
| 210KB 源生成 C 代码体积 | ≈ 2.10 MB | ≈ 1.15 MB | **−45%** |
| 大文件 `vus build --exe` 编译耗时 | ≈ 14.3 s | ≈ 10.2 s | **−29%** |
| 热循环运行耗时（`hot2.vus`，50×50000 迭代，10⁷ 级加法/赋值） | ≈ 1.67 s | ≈ 0.88 s | **+47%** |

三层优化的关系：生成代码越紧凑 → GCC 解析/优化负担越低 → 编译越快（−29% 主要来源）；运行时热路径（字符串构造、变量赋值、数值转文本）越短 → 解释型语义下的执行越快（+47% 主要来源）。

---

## 二、生成代码优化（`src/generator.c`）

### 2.1 函数体特征扫描（按需生成变量与模板）

- 改动前：每个函数固定生成 `_vus_err`、`_vus_result` 变量及对应错误链/返回值模板，即使函数**没有** `返回`、**没有** `尝试/捕获`。
- 改动后：在生成前扫一遍函数体（`gen_*` 遍历 `VusAstFunctionDef` 语句），按特征**按需生成**：
  - 出现 `返回` → 生成 `_vus_result` 与返回模板；
  - 出现 `尝试`/`抛出` → 生成 `_vus_err` 与错误链模板。
- 收益：无返回/无异常的函数（如初始化、纯副作用函数）体积显著下降，大文件中此类函数占多数。

### 2.2 变量赋值热路径 `vus_var_set`

- 改动前：`x = 值` 生成 3~5 步（取值 → 判旧值 → `vus_unref` 旧值 → `vus_ref` 新值 → 回写）。
- 改动后：新增运行时函数 `vus_var_set(VusString **slot, void *newval)`，生成代码一行调用，ref/unref 在函数内一次完成。
- 收益：赋值路径代码量下降；循环内变量引用切换不再重复构造/转换。

### 2.3 列表/字典字面量 helper（`vus_object_list` / `vus_object_dict`）

- 改动前：`[a, b, c]` 被逐元素展开为冗长的"obj = vus_object_list(); vus_list_append(...); vus_ref(...)..."序列。
- 改动后：新增 `vus_object_list(void **items, int n)` / `vus_object_dict(...)`，字面量收敛为一次调用 + 参数数组。
- 收益：字面量密集代码（配置表、控件参数表）体积大幅缩小。

### 2.4 整数字面量池化

- 整数常量（含循环边界、下标）生成 `vus_literal("7")` 而非 `vus_string_new("7")`——走字符串常量池，多次同值共享一个实例。
- 收益：重复字面量不再各自 `malloc` + `memcpy`，生成代码与内存双降。

### 2.5 转文本转发（`vus_to_string` 直通）

- `转文本(入参)` 在入参已是字符串时直接转发引用，避免"先构造中间串再驻留"的冗余路径。

### 2.6 循环模板收敛（防缩进爆炸）

- 改动前：`for_range`/`for_each` 把循环体按行展开进 C 代码，循环嵌套时产生**按嵌套深度指数增长的缩进/块嵌套**。
- 改动后：循环模板收敛为单行紧凑块：
  - 循环值只构造一次（不再每次迭代重建）；
  - 块体合并为单行写入（避免缩进爆炸）。
- 收益：深层循环的 C 代码体积与 GCC 解析耗时显著下降。

### 2.7 `omit_main` 库式编译

- `VusConfig.omit_main` 为 1 时不生成 `main`（.vusx 插件、VUA/库式编译用），避免与宿主程序 `main` 多重定义冲突。

### 2.8 调用点引用计数对称归还（函数边界，R1）

- 改动前：函数入口对参数 `vus_ref`（防函数内改写把调用方引用 `unref` 掉）但调用边界从不归还；`返回` 处 `vus_var_set(&_args[0], …)` 与调用点 `vus_var_set(&目标, 返回值)` 各多计一份。效果：共享实参（池/变量）引用计数**永久虚高**，动态实参（拼接/转换结果）每调用永久 +1（真实泄漏）。
- 改动后：调用点语句表达式内对称归还
  - 实参：`vus_unref(_vus_args[1..N])`（配平入口 `vus_ref`）——**不受函数内改写/早退 `return` 影响**（归还的是入口对传入对象加的引用，函数体改的是局部指针）；
  - 返回槽：`VusString* _vr=<取>; vus_unref(_vr); _vr;`（配平 return 处 `vus_var_set(&_args[0],…)`）。
- 收益：函数调用路径引用计数精确——共享实参/返回零残留；**仅**「返回新建临时对象 / 传入新建临时对象」时残留构造的 1 份（见 3.7 待办 R6）。
- 结构体构造（`VUS_AST_STRUCT_INSTANTIATE`）**不**归还：字段 `vus_ref` 是有意持有（保障字段不被调用方改写连坐释放），归还会破坏持有语义。

### 2.9 字面量一次性静态缓存（R2）

- 改动前：每个字面量表达式每次求值都调 `vus_literal`（`strlen` + 全字节哈希 + `memcmp` 兜底），热循环内同一常量每迭代重复 O(len)。
- 改动后：生成 `({static VusString* _Ln=NULL; if(!_Ln){_Ln=vus_literal("…"); vus_ref(_Ln);} _Ln;})` —— 首次入池一次，热路径成本降为一次判空；静态持有自己的一份引用，即使池槽被其他字面量换出也不悬垂。
- 基准（`tests/tmp_litbench.vus`，10⁷ 迭代 × 10 字面量，本机）：1358ms → 774ms（**+43%**）。

### 2.10 连续拼接链折叠（R3）

- 改动前：`a .. b .. c .. d` 展开为 `vus_string_concat` 嵌套链（左结合），K 段 = K 次 malloc + 2K 次 memcpy；f-string 插值链（parser 侧已串成 `..` 链）同样受累。
- 改动后：生成器对 `..` 链**展平**为平铺段数组，K≥3 时一步生成 `({VusString* _vp[N]={…}; vus_string_concat_n(_vp, N);})` —— 单次 `malloc` + N 次 `memcpy`；K=2 保持既有 2 参 `vus_string_concat`（等成本）；单段直接返回表达式。展平分支提前于左右子树 `gen_expr`，避免重复生成（f-string 收益叠加）。
- 基准（`tests/tmp_c3.vus`，2×10⁶ 次 × 4 段拼接，本机）：462ms → 259ms（**+44%**）。

### 2.11 变量赋值同指针短路（R5）

- `vus_var_set` 开头新增 `if ((void*)*slot == v) return;`：循环内反复赋同一变量/常量时跳过 ref/unref 抖动（同对象 ref+1 再 unref-1 净零，短路语义完全等价）。共享实例（池/驻留缓存/静态字面量）在热路径上免去两次数操作。

---

## 三、运行时优化（`rt/libvus_rt.c`）

### 3.1 字符串常量池 `vus_literal`

```c
VusString* vus_literal(const char* s);
```

- 以「哈希槽」缓存字面量字符串，命中（长度 + `memcmp`）直接返回共享实例。
- 应用：高频重复的键名/常量（如 `界面_设置("计数", v)` 的变量名、循环内重复整数文本）。
- 注意：常量池实例为共享引用，调用方仍应遵循既有 ref/unref 约定（池内持有引用）。

### 3.2 字符串拼接免二重复制 `vus_string_concat`

- 改动前：先算出总长 → 分配 → 两次 memcpy 拼接 → 中间多有临时缓冲。
- 改动后：直接分配目标 `VusString`（单块 malloc），一次性把两段数据写入 `data`，去掉中间缓冲与二次复制。

### 3.3 `VusString` 头 + 负载单块 malloc

- 结构体头（引用计数/长度/指针）与 `data` 负载一次 `malloc` 分配（头紧随负载），减少分配次数与缓存跳跃。

### 3.4 `vus_to_string` 整数值驻留缓存

- 高频小整数（如循环计数 `0..N`、下标）的文本结果缓存，热循环中避免反复 `snprintf`/`malloc`。
- 收益：`hot2.vus` 类循环里 `转文本(总和)`/`转数字("7")` 等高频路径占大头，命中缓存后耗时几乎归零。

### 3.5 变量赋值热路径 `vus_var_set`

- 与生成代码 2.2 配套：统一"新值 ref → 旧值 unref → 回写指针"，规避竞态与重复判断。

### 3.6 `vus_to_string` 长度优化

- 整数转文本按最坏位数精确分配，去除额外增长空间冗余。

### 3.7 容器序列化缓冲化 + 字典真实序列化（R4）

- 改动前：`vus_object_to_string` 的列表分支逐元素「new 分隔符 + concat」，n 元素 ≈ 2n 次 malloc；字典分支返回 `"{}"` 占位（真实内容缺失）。
- 改动后：
  - 列表/字典序列化改**增长缓冲**一次拼装，末尾单次 `vus_string_new_len` 收包（n 元素：O(n) 拷贝 + 少量 realloc，远低于 2n 次分配）；
  - 字典输出真实内容 `{k: v, ...}`（键值递归序列化，嵌套容器支持），空容器保持 `[]`/`{}`。
- 行为说明：VUS 的 `打印`/`..`/`转文本` 对容器均需先经 `对象文本`（`转文本` 是直通转发，容器不可直接拼接——既有语义）。

### 3.8 已知遗留：表达式临时对象无主计数（R6，待办）

- 现象（经对照探针定位）：`x = x + 1`、`转数字(s)` 等**新建**结果在赋值/丢弃后残留构造的 1 份引用（`vus_string_new` 初值为 1 属「所有权随指针」语义，但 `vus_var_set` 无差别 +1 且无处归还），实测 2×10⁷ 次 `总和 = 总和 + 1`（超出小整数缓存范围的值）RSS 增长 ≈ 780MB（改前后一致，与函数边界无关）。
- 共享对象（池/变量）不受影响（引用精确配对，R1 已覆盖函数边界）。
- 根治方案（R6，独立立项）：生成器对「必新建」表达式（二元/一元运算、内建调用、列表/字典字面量等）在语句粒度做自动 `vus_unref`（RAII 式丢弃）；需配套「移动 vs 共享」信息流判断（如 `转文本(字符串)` 直通转发、`列表_取` 借用返回等不可移动），改动面大，暂缓。

---

## 四、编译器速度

- 体积下降直接降低 GCC 前端词法/语法/优化负担：大文件编译 **−29%**。
- 构建脚本对运行时采用预归档 + 差异策略：`libvus_rt.a` 一次编译，业务函数增量编译，避免"每轮全量重链运行时（含 yyjson/EasyLogger/GuiLite）"。

---

## 五、基准复现

```bash
# 1) 编译基准（big.vus 约 210KB）
time vus build --exe .bench/big.vus

# 2) 热循环运行基准（输出结果应为 "0" 或固定总和文本）
time vus run .bench/hot.vus    # 30 × 2000 次迭代
time vus run .bench/hot2.vus   # 50 × 50000 次迭代（10⁷ 级加法）

# 3) 生成代码体积
vus build --c-only .bench/big.vus
ls -la 构建/*.c
```

> 数值会随机器波动；对比时应使用同一台机器、同一组 `CFLAGS`（默认 `-O2`）与同一编译器版本，多次取中位数。

---

## 六、与旧文档口径的差异说明

- 早期架构文档（优化前）描述的"逐函数固定生成 `_vus_err`/`_vus_result`"已由 2.1 取代。
- 早期 `vus_to_string`/`vus_add` 的中间缓冲区路径已由 3.2/3.4 取代；`vus_add` 仍保留整数算术/字符串拼接的智能分派语义（生成代码负责分派，运行时负责拼接）。

---

## 七、编译器前端优化（A1-A5，2026-09 专项）

> 目标：消除巨型源文件下的 O(n²) 前端扫描、重复子进程探测、硬上限静默截断，
> 以及增量缓存未前置导致的"不改源码也全量跑前端"。

### 7.1 A1 名称查表哈希化（generator.c）

- 改动前：`scope_find`/`scope_find_current`/`namereg_find`/`gen_is_global_name` 均为
  线性扫描，巨型函数（数百局部 × 数百块引用）下叠加为 O(n²) 次 `strcmp`。
- 改动后：全局名、作用域符号、局部名分类三张表全部改为 256 槽链式哈希桶
  （`gen_name_hash` djb2 变体 + 桶头/桶内 next 双数组），查找收敛为 O(1)-ish。
- **修复的隐藏缺陷**：符号表保存未取模哈希（`1314343090` 全值）而查找用取模后值
  （`% 256`），比对恒不等 → `scope_find` 恒 miss，靠调用方防御性重复声明兜底，
  大文件下产生 redefinition 错误。统一"存储时即取模"后，500 局部 × 5 函数规模文件
  从"无法编译"变为可编译可运行。
- 另修复 `scope_reset` 未清零每层 `count` 的跨函数符号计数残留（巨型文件误触上限）。

### 7.2 A2 增量缓存判断前移（main.c）

- 改动前：`vus_compile_to_exe` 先全量跑前端（lex/parse/生成写 C），再检查
  `构建/<名>` 与源文件 mtime 决定是否跳过 GCC —— 不改源码也全量跑前端。
- 改动后：缓存判断（exe mtime ≥ 源 mtime 且 `libvus_rt.a` 未更新）提前到函数
  入口 **`vus_compile_to_c` 之前**，命中直接返回 exe 路径。
- 实测：命中缓存从 ~1.0s（前端+链接）降到 **~8ms**。

### 7.3 A4 外部探测子进程结果缓存（generator.c）

- 每次 `vus_compile_c` 都会 `popen` 4-6 个外部探测：`curl-config`、
  `python3-config --includes/--ldflags`、X11/Xft 头 `test`、`pkg-config freetype2`。
  同进程内多次编译（LSP 校验、多文件构建、vusx 链）反复拉起子进程。
- 改动后：六个探测封装为 static 一次性缓存（编译会话内环境不变假设），
  之后同进程再编译零子进程开销。

### 7.4 A5 硬上限静默截断 → 显式报错 + 下溢守卫（generator.c）

- 名称表容量提升：全局名 512→8192、局部名分类 1024→4096、提升表 512→4096。
- 原"超限静默丢弃声明"（`return` 直接截断）改为 `gen_error` 显式报告
  （"全局变量数量超过 8192 上限" / "单函数局部变量超过 4096 上限" 等），
  不再产出语义错误的 C 代码。
- 参数/返回拼接缓冲区 `GEN_APPEND` 宏：`pos` 累加超过 `sizeof(b)` 后若仍以
  `sizeof(b)-pos` 续写会 **size_t 无符号下溢 → snprintf 越界写栈**；改为满则停
  并报 "代码生成实参缓冲区溢出"。五处 `args_buf` 全部换用。

### 7.5 A3 生成器分配 churn（评估结论：暂缓）

- 现象：每个表达式 `strdup` + 多次 `memcpy` 拼接，生成期分配量为输出 C 的
  3-10 倍（泛型实例化重复生成放大）。
- 评估：A3 涉及生成器核心（GenBuf 表达式拼接模型）重构，风险集中在语义等价性；
  而实测同规模文件下 **GCC 编译生成 C 占总耗时 >85%**，前端优化收益在 GCC 面前
  不显著。**结论：暂缓实施**，待前端耗时占比提升（或新增耗时敏感场景）后立项。

### 7.6 收益基准（本机，`-O2`）

| 负载 | 优化前 | 优化后 | 说明 |
|------|-------:|-------:|------|
| 4629 行（300 全局 + 3×240 局部）首次全链路 | ≈2.35s | ≈2.35s | GCC 主导，前端差异 <2% |
| 15395 行（380 全局 + 5×500 局部）首次全链路 | 无法编译 | ≈5.4s | A1 修复 + A5 显式上限后能力解锁 |
| `vus run` 二次运行（源码未改，A2 缓存） | ≈1.0s | ≈8ms | 前端+链接全部跳过 |

> 前端 O(n²)→O(n) 的结构性收益在"大文件首次编译"里被 GCC 链接时间掩盖；
> 实际价值体现为**规模解锁**（此前编译失败的文件现可正常编译）与
> **反复运行/多文件构建/LSP** 场景（A2/A4 消除重复工作）。